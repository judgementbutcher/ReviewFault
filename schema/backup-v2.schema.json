{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://reviewfault.local/schema/backup-v2.schema.json",
  "title": "ReviewFault backup manifest v2",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "format",
    "version",
    "appVersion",
    "schemaVersion",
    "schedulerAbiVersion",
    "exportedAt",
    "files"
  ],
  "properties": {
    "format": { "const": "reviewfault-backup" },
    "version": { "const": 2 },
    "appVersion": { "type": "string", "pattern": "^0\\.2\\.[0-9]+(?:[-+][A-Za-z0-9.-]+)?$" },
    "schemaVersion": { "const": 2 },
    "schedulerAbiVersion": { "const": 2 },
    "exportedAt": { "type": "integer", "minimum": 1 },
    "files": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["path", "sha256", "bytes"],
        "properties": {
          "path": {
            "type": "string",
            "pattern": "^(?:database\\.sqlite|media/(?!\\.\\.(?:/|$))(?!.*?/\\.\\.(?:/|$))[A-Za-z0-9._/-]+)$"
          },
          "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
          "bytes": { "type": "integer", "minimum": 0 }
        }
      }
    }
  }
}
