# v0.2 调度基线

本报告冻结 v0.2 参数的匿名回放基线。输入为
`fixtures/replay_baseline_v2.tsv`，只包含内容类型、时间、成功标记、预测召回率和调度间隔，
不包含题目、答案、图片、标签或设备标识。

运行：

```sh
node --no-warnings tools/replay-evaluate.mjs \
  --input fixtures/replay_baseline_v2.tsv
```

冻结指标：12 次事件，召回率 66.67%，遗忘 4 次，单日峰值 3 次；间隔最小值 0.00694 天、
中位数 2.3065 天、P90 14 天、最大值 30 天。Brier 校准误差由命令逐事件计算，后续参数实验
必须在真实本地历史、黄金样例和极端边界上同时不劣于该基线，不能用单一平均间隔替代验收。
